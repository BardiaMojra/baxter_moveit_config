robot_description_planning:
  cartesian_limits:
    max_trans_vel:  1.0    # m/s   (actual speed capped by velocity_scaling_factor in script)
    max_trans_acc:  2.25   # m/s^2
    max_trans_dec: -5.0    # m/s^2 (negative = deceleration)
    max_rot_vel:    1.0    # rad/s
